# Database - regenerated by the collector, never versioned
data/*.db
data/*.db-journal
data/*.db-wal # alternative to journal above, where the main .db file isn't touched during a writing of new info at all. New changes are appended to a .db-wal file, then the log's contents are folded back into the main file and truncating it.
data/*.db-shm #


# Secrets - add the super duper secret api key
.env


__pycache__/
*.pyc
.venv/
venv/

.DS_Store
